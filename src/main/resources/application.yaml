spring:
  application:
    name: libraryhtn
  datasource:
    url: jdbc:postgresql://localhost:5442/librarydb
    username: user
    password: password
    driver-class-name: org.postgresql.Driver
  liquibase:
    enabled: true
    change-log: classpath:db/changelog/changelog-master.yml
